# Example 1 — New deck, two slides, one title

A minimal smoke replay: agent creates a presentation, adds a slide, sets a title.

This case has no `expected` — it's a Replay (community contribution shape per spec 3.4), not a Benchmark.
